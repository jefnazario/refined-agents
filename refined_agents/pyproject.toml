[project]
name = "refined-agents"
version = "0.1.0"
description = "Prompt generator for coding agents"
requires-python = ">=3.11"
dependencies = [
    "dynaconf>=3.2.13",
]

[tool.uv]
package = false
